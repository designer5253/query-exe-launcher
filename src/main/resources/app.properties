# Self-describing launcher version — same idea as the client's app.version.
# Forwarded to the spawned client as -Dlauncher.version so it can tell whether
# the launcher that started it needs updating.
launcher.version=${project.version}

# No hub: both sides query GitHub Releases directly for update checks.
github.owner=${github.owner}
github.repo.client=${github.repo.client}
